<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8" />
    <title>Phiếu <%= serviceName %></title>
    <style>
        body {
            font-family: "Times New Roman", 'Liberation Serif', 'Noto Serif', serif;
            font-size: 14px;
            margin: 40px;
            line-height: 1.5;
        }
        p {
            margin: 0;
        }
        h2 {
            text-align: center;
            text-transform: uppercase;
            font-size: 18px;
            margin-bottom: 5px;
        }

        div {
            margin-bottom: 8px;
            margin-top: 8px;
        }

        div > div {
            margin-top: 1px;
            margin-bottom: 1px;
        }

        .header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        .header .left {
            width: 60%;
        }
        .header .right {
            text-align: right;
            width: 40%;
        }


        .section {
            margin-top: 10px;
        }
        .box {
            border: 1px solid #000;
            padding: 8px;
            min-height: 50px;
            margin: 10px 0;
        }
        .images {
            display: grid;
            gap: 12px;
            align-items: start;
        }

        .images:has(img:only-child) {
            grid-template-columns: repeat(2, 1fr);
        }

        .images:has(img:nth-child(2)):not(:has(img:nth-child(3))) {
            grid-template-columns: repeat(2, 1fr);
        }

        .images:has(img:nth-child(3)):not(:has(img:nth-child(4))) {
            grid-template-columns: repeat(3, 1fr);
        }

        .images:has(img:nth-child(4)) {
            grid-template-columns: repeat(4, 1fr);
        }

        .images img {
            width: 100%;
            height: auto;
            display: block;
            object-fit: cover;
            border-radius: 6px;
            border: 1px solid #ccc;
        }

        .signature-row {
            display:flex;
            justify-content:space-between;
            align-items:flex-start;
            margin-top: 20px;
            font-size:13px;

            page-break-inside: avoid;
            page-break-before: avoid;
            break-inside: avoid;
            break-before: avoid;
        }
        .sig-right {
            text-align:center;
            width:40%;
        }
        .signature-line {
            margin-top:36px;
            width: 80%;
            margin-left: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }
        td {
            vertical-align: top;
            padding: 3px 0;
        }
        .footer {
            margin-top: 20px;
            display: flex;
            justify-content: space-between;
        }
    </style>
</head>
<body>
<div class="header">
    <div class="left">
        <p>Sở Y tế: <%= departmentOfHealth%></p>
        <p>BV: <%= hospitalName %></p>
    </div>
    <div class="right">
        <p>Mã số: MS:08/BV-02</p>
        <p>Số: <%= recordNumber%></p>
    </div>
</div>

<h2>PHIẾU <%= serviceName.toUpperCase()%></h2>

<div>
    <table>
        <tr>
            <td>Khoa: <%= specialization %></td>
            <td>Phòng: <%= locationName %></td>
        </tr>
        <tr>
            <td>Họ tên người bệnh: <strong><%= patientName %></strong></td>
            <td>Ngày sinh: <%= patientBirth %></td>
            <td>Nam/Nữ: <%= patientGender %></td>
        </tr>
        <tr>
            <td colspan="3">Địa chỉ: <%= patientAddress %></td>
        </tr>
    </table>
</div>

<div class="section">
    <strong>Yêu cầu chiếu/chụp</strong>
    <div class="box">
        <%= request %>
    </div>
</div>

<div class="signature-row">
    <div></div>
    <div class="sig-right">
        <p><%= requestTime %></p>
        <p><strong>Bác sĩ điều trị</strong></p>
        <div class="signature-line"></div>
        <p><%= specialDoctor %></p>
    </div>
</div>

<div class="section">
    <strong>Kết quả chiếu/chụp</strong>
    <div class="box">
        <div style="margin-bottom: 10px;" class="images {{images.length > 4 ? 'many' : 'few'}}">
            <% for (var i = 0; i < media.length; i++) { %>
                <img src="<%= media[i] %>" alt=""/>
            <% }%>
        </div>
        <%- reportContent %>
        <p>Diễn giải, giải thích ảnh: <%- reportInterpretation %></p>
    </div>
</div>

<div class="signature-row">
    <div></div>
    <div class="sig-right">
        <p><%= reportResultTime %></p>
        <p><strong>Bác sĩ thực hiện </strong></p>
        <div class="signature-line"></div>
        <p><%= performDoctor %></p>
    </div>
</div>

</body>
</html>
